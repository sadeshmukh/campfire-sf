---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Join our Slack">
	<main>
		<section class="slack-page">
			<a href="/" class="back-link">&larr; </a>
			<h1 class="page-title">Join the Campfire Slack</h1>
			<div class="instructions-container">
				<p>
					Our slack is where all of our communications come through:
				</p>
				<ol>
					<li>Fill out the form on the tab that just opened</li>
					<li>Verify your Email</li>
					<li>Once you are in, accept the terms and conditions in your DMs.</li>
					<li>Join the channels #campfire-sf and #campfire-sf-announcements.</li>	
					
				</ol>
				<a href="https://auth.hackclub.com/slack" class="slack-btn" target="_blank" rel="noopener noreferrer">Join Now</a>
			</div>
		</section>
	</main>
</Layout>

<style>
	.back-link {
		position: absolute;
		top: 2rem;
		left: 2rem;
		color: var(--cream);
		text-decoration: none;
		font-size: 1.1rem;
		font-family: sans-serif;
		background: rgba(255, 255, 255, 0.1);
		padding: 0.5rem 1rem;
		border-radius: 8px;
		transition: background 0.2s ease;
	}

	.back-link:hover {
		background: rgba(255, 255, 255, 0.2);
	}

	.slack-page {
		position: relative;
		padding: 6rem 2rem;
		background-color: var(--starfuel);
		color: var(--cream);
		min-height: 100vh;
		display: flex;
		flex-direction: column;
		align-items: center;
	}

	.page-title {
		font-family: 'Dreamplanner', sans-serif;
		font-size: clamp(2.5rem, 6vw, 4rem);
		text-align: center;
		margin-bottom: 3rem;
		text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
	}

	.instructions-container {
		max-width: 800px;
		margin: 0 auto;
		font-size: 1.2rem;
		background: rgba(255, 255, 255, 0.05);
		padding: 2rem 3rem;
		border-radius: 16px;
		text-align: center;
	}

	.instructions-container p {
		margin-bottom: 2rem;
	}

	.instructions-container ol {
		list-style-position: inside;
		text-align: left;
		display: inline-block;
		margin-bottom: 2.5rem;
	}

	.instructions-container li {
		margin-bottom: 1rem;
		line-height: 1.6;
	}

	.slack-btn {
		padding: 1rem 2.5rem;
		background-color: var(--orangesicle);
		color: var(--cream);
		font-family: 'Dreamplanner', sans-serif;
		font-size: clamp(1.2rem, 2.5vw, 1.5rem);
		border: none;
		border-radius: 12px;
		cursor: pointer;
		box-shadow:
			0 4px 0 var(--smores),
			0 6px 12px rgba(0, 0, 0, 0.2);
		transition:
			transform 0.15s ease,
			box-shadow 0.15s ease;
		text-decoration: none;
	}

	.slack-btn:hover {
		transform: translateY(-2px);
		box-shadow:
			0 6px 0 var(--smores),
			0 8px 16px rgba(0, 0, 0, 0.25);
	}
</style>
